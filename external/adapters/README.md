# External adapters

Adapters in this directory communicate with separately installed applications
through CLI, MCP, HTTP, or another documented process boundary. They must not
import non-MIT external source into the ScoreSymphony core.
