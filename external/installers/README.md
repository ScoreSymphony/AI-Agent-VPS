# External installers

Installers are not implemented in the baseline. Future installers must use a
pinned source, display the original license, verify provenance, install outside
the Git checkout, run a health check, and emit audit events.
